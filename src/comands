sudo docker-compose up -d database       // subir el contenedor
sudo docker-compose ps                   // Listar contenedores
sudo docker-compose exec database bash   // entrar al contenedor
psql -h localhost -d my_db -U root       //Entrar a gestionar la bd